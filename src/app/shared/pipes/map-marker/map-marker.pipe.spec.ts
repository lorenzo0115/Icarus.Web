import { MapMarkerPipe } from './map-marker.pipe';

describe('MapMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new MapMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
