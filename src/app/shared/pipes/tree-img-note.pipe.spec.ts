import { TreeImgNotePipe } from './tree-img-note.pipe';

describe('TreeImgNotePipe', () => {
  it('create an instance', () => {
    const pipe = new TreeImgNotePipe();
    expect(pipe).toBeTruthy();
  });
});
